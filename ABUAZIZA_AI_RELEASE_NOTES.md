# Abuaziza AI v1.1 — متعدد الاستخدام

تم تطوير النموذج ليصبح تطبيقاً واحداً بثلاثة مسارات:

1. **المحادثة:** مساعد عربي محلي للطلبات العامة والبرمجة والتحليل والكتابة.
2. **التنقيب:** تقييم شفاف لمؤشرات الذهب السطحية مع عرض الأدلة الناقصة.
3. **حول التطبيق:** سياسة الخصوصية والدقة وحدود النموذج.

## التشغيل والإصدار

1. ادمج ملفات `android-engine` داخل مشروع Android الذي ينشئه Workflow.
2. شغّل Workflow البناء من تبويب Actions.
3. حمّل APK من Artifacts.
4. أنشئ GitHub Release من APK بعد اختبار التثبيت على جهاز فعلي.

لا تضع كلمات مرور التوقيع داخل YAML. استخدم GitHub Actions Secrets:

```text
CI_KEYSTORE_PASSWORD
CI_KEY_PASSWORD
CI_KEY_ALIAS
```

## حدود الذكاء

هذا نموذج محلي قابل للتطوير، وليس دليلاً علمياً على وجود الذهب. دقة تقييم التنقيب تتحسن فقط بإضافة بيانات موثقة: صور مؤرخة، مواقع، قياسات متكررة، نوع الصخور، عينات، وتحاليل مخبرية.
